import VoiceApp from "../components/VoiceApp";

export default function Home() {
  return (
    <main className="min-h-screen bg-base flex flex-col items-center px-4 py-8">
      <VoiceApp />
    </main>
  );
}